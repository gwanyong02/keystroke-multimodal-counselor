# 키스트로크 멀티모달 심리 상담 LLM

## 사용자
현재 Claude Code를 사용하는 사람은 박관용(A)이다.
담당 모듈: 키스트로크 감정 분류기, 프롬프트 조립, LLM 파이프라인.

## 팀원 및 역할
팀원 역할 분담이 필요하면 README.md를 읽어라.

## 참고 문서
프로젝트 아키텍처, 진행사항, 데이터셋, 평가 방법론이 필요하면
README.md를 읽어라.
인터페이스 스펙(모듈 간 JSON 형식)이 필요하면
interface_spec.md를 읽어라.
먼저 읽지 말고, 작업 중 필요하다고 판단될 때 읽어라.

## 주의사항
- EmoSurv CSV는 .gitignore 대상이므로 절대 커밋하지 않는다
- interface_spec.md 스펙 변경 시 반드시 팀 공유 후 진행한다
- participant_session_mapping 파일은 레포에 포함하지 않는다
